# son funciones que ya vienen integradas EJ

str('56')

round(4.8) #redondea el valor a 5

eval('2+5') #Evalua si dentro de una cadena de caracteres hay una
# operacion aritmetica que se pueda resolver 
n=10
eval('n * 10 -5')
abs(-20) #devuelve el valor absoluto
bin(10) #vulve un numero a binario
# si ponemos un valor binario a int lo convierte al valor original 
hex(10) # convierte un valor a exadecimal, tambien se puede pasar este resultado a int y devulve el valor original = 10

# si usamos help() nos muestra la documentacion de PY donde podemos buscar 
# lo que necesitamos especificamente
# para salir seria ctrl + z o exit()