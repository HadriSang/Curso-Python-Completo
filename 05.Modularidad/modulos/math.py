import math as m


m.floor(4.99) # Este lo que hace es redondear hacia abajo es decir 4

m.ceil(4.01) # este redondea hacia arriba es decir 5

n = [0.999,1,2,3]

m.fsum(n) #Devuelve la suma de la lista n respetando los decimales

m.trunc(45.785) #resumido ignora los decimales y devuelve el valor entero

m.pow(2, 3) # esto lo que hace es 2 elevadoa  la 3 igual 8

m.sqrt(9) #Devuelve la raiz cuadrada

m.pi #devuelve el valor de pi

m.e # Devuelve el valor de euler